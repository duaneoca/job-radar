apiVersion: v1
kind: ConfigMap
metadata:
  name: mcp-writer-config
  namespace: jobradar
data:
  # In-cluster service name — no port needed (Service exposes :80 → :8000)
  TRACKER_API_URL: "http://jobradar-tracker-api"
