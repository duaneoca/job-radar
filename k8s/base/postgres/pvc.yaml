apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: jobradar
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path   # k3s default provisioner
  resources:
    requests:
      storage: 2Gi
