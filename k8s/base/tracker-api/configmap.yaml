apiVersion: v1
kind: ConfigMap
metadata:
  name: tracker-api-config
  namespace: jobradar
data:
  ENVIRONMENT: "production"
  REDIS_URL: "redis://redis:6379/0"
  TRACKER_API_URL: "http://jobradar-tracker-api"
  # DATABASE_URL and SECRET_KEY are in secrets
