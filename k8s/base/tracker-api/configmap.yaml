apiVersion: v1
kind: ConfigMap
metadata:
  name: tracker-api-config
  namespace: jobradar
data:
  ENVIRONMENT: "production"
  REDIS_URL: "redis://redis:6379/0"
  # DATABASE_URL is in secrets (contains credentials)
