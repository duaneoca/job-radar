apiVersion: v1
kind: Service
metadata:
  name: jobradar-frontend
  namespace: jobradar
  labels:
    app: frontend
    project: jobradar
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
