apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: ""
namespace: jobradar-production

bases:
  - ../../base

# Production runs 2 replicas of the API (matches base default)
# Scale up here when traffic warrants it
