# ─────────────────────────────────────────────────────────────
# Deploy to Production — triggers on Git tag v*.*.*
# Promotes the tagged images from staging to production.
#
# Usage:
#   git tag v1.0.0
#   git push origin v1.0.0
# ─────────────────────────────────────────────────────────────

name: Deploy — Production

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}/jobradar

jobs:

  tag-and-deploy:
    name: Tag images and deploy to production
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: write
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Retag images for production
        # The staging images were already built; we just retag them
        run: |
          set -euo pipefail   # a failed pull/tag/push for ANY service fails the deploy
          for svc in tracker-api scraper ai-reviewer mcp-writer frontend; do
            docker pull ${{ env.IMAGE_PREFIX }}-${svc}:staging
            docker tag  ${{ env.IMAGE_PREFIX }}-${svc}:staging \
                        ${{ env.IMAGE_PREFIX }}-${svc}:${{ github.ref_name }}
            docker tag  ${{ env.IMAGE_PREFIX }}-${svc}:staging \
                        ${{ env.IMAGE_PREFIX }}-${svc}:latest
            docker push ${{ env.IMAGE_PREFIX }}-${svc}:${{ github.ref_name }}
            docker push ${{ env.IMAGE_PREFIX }}-${svc}:latest
          done

      - name: Set up kubectl
        uses: azure/setup-kubectl@v4

      - name: Configure kubeconfig (production)
        run: |
          mkdir -p ~/.kube
          printf '%s' "${{ secrets.KUBECONFIG_PRODUCTION }}" > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: Deploy with Kustomize
        run: kubectl apply -k k8s/overlays/production

      - name: Update image tags
        run: |
          set -euo pipefail
          for svc in tracker-api ai-reviewer scraper mcp-writer frontend; do
            kubectl set image deployment/jobradar-${svc} \
              ${svc}=${{ env.IMAGE_PREFIX }}-${svc}:${{ github.ref_name }} \
              -n jobradar-production
          done

      - name: Wait for rollout
        # Verify EVERY service rolls out — a stuck/ImagePullBackOff service (e.g. an
        # image the retag step failed to push) now fails the deploy instead of hiding.
        run: |
          set -euo pipefail
          for svc in tracker-api ai-reviewer scraper mcp-writer frontend; do
            kubectl rollout status deployment/jobradar-${svc} \
              -n jobradar-production --timeout=180s
          done

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
